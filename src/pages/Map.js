import React from "react";

const Map = () => {
  return (
    <div className="map">
      <h1>Definition and Usage</h1>
      <h3>
        map() creates a new array from calling a function for every array
        element.
      </h3>
      <h3>map() calls a function once for each element in an array.</h3>
      <h3>map() does not execute the function for empty elements.</h3>
      <h3> map() does not change the original array.</h3>

      <h2>Syntax </h2>
      <h3>array.map(function(currentValue, index, arr), thisValue)</h3>
      <code>Hello</code>
    </div>
  );
};

export default Map;
